
function takeOrder(order, orderList) {
  if (orderList.length < 3) {
    orderList.push(order);
    return orderList;
   } //else {
  //   // if (orderList.length > 3);
  //   // return orderList;
  // }
  // // orderList.push(order);
}








module.exports = {
  takeOrder,
  // refundOrder,
  // listItems,
  // searchOrder
}
